Forside: push, fordi de to ting ordren spurgte til holder: dagene er umiskendelige (form, ikke kun farve) med 8 px luft, overskriften siger altid hvilken dag, "Mere" virker, pas/pause/ret sæt/næste øvelse er intakte, og toasten dækker ikke topbaren. Fundene nedenfor er middel/lav og kan rettes efter push.
Skak: klar til Marc, fordi drag i Lær skak (forkert afvist og forklaret, rigtigt godkendt), Opstil-drag, gåder, motor, tegnelag og Opgave-fanen opfører sig efter modulopdelingen, og konsollen er tom. Ét middel-fund ("Løs gåder" efter et spil) findes også før opdelingen, så det er ikke 326's.

Målt med `node scripts/kritik-334.mjs` (headless Chromium, touch, mock; ingen atletdata). Skærmbilleder i `outputs/334/` (`f-*` forside, `s-*` skak). Skak er testet på `git show 288c95a:skak.html` (main), ikke på skak-repoets arbejdstræ.

## Fund efter alvor

### Middel

**F1. Strimlens tekst er for lille til en telefon.** `src/AthleteView.jsx` `WeekCalendar`: ugedagsforkortelsen er 7,7 px og "hvile"-teksten 6,4 px (målt begge viewports). Hvile/pas-forskellen bæres delvist af netop den tekst (plus stiplet kant). Skærmbillede: `f-390x844-01-forside.png`. Kant, cirkel og trekant bærer stadig dagen uden tekst, så "umiskendelig" holder, men teksten kan ikke læses uden at zoome.

**F2. På 360 px er dagcellerne 40 px brede** (390 px: 44). Under 44 px tap-mål; cellerne er 65 px høje, så det er bredden der mangler. Mistænkt: `gridTemplateColumns: repeat(7, 1fr)` + `gap: 0.5rem` i `WeekCalendar`. Skærmbillede: `f-360x780-01-forside.png`.

**F3. "Tre sekundære ting over folden" holder ikke, når der er sæt logget.** Hvert loggede sæt får en "ret"-række (~50 px) i pas-kortet, så Parathed/Besked/Film et sæt og "Mere" skubbes under folden: på 360x780 allerede med to sæt logget (Tirsdag-mock), på 390x844 er "Mere" 905 px nede (fold 844). RAPPORT-330's "3 over folden" er målt på et pas uden loggede sæt. Parathed-chippens "Ikke logget"-nudge er dermed ikke synlig midt i et pas. Skærmbilleder: `f-360x780-01-forside.png`, `f-390x844-05-efter-saet-hele.png`. Mistænkt: `DagensPasCard` (rækkerne "Ret sæt n"), `src/AthleteView.jsx` chips ~l. 3826.

**F4. "Løs gåder" efter et spil viser spilbrættet, ikke en gåde** (skak). Spil et træk mod computeren, skift til "Løs gåder": brættet er spilstillingen, "Vis et hint" peger på meningsløse felter (f.eks. f7→f2). Identisk på `4215ec2` (før opdelingen), så ikke en 326-regression, men Marc rammer det ved almindelig brug. Skærmbillede: `s-6-gaade-efter-spil.png`. Ny session med gåden som første fane virker fint.

### Lav

**F5. Toasten dækker overskriften.** "Ny personlig rekord" ligger under topbaren (top 61,6 mod 53), men over h1 ("Tirsdag · Dag 2 …", 77–105) og delvis strimlen i ca. 3,2 s, og på 360 px når siden er ruller lidt (scrollY 111) ligger den ovenpå strimlen. Præcis det øjeblik atleten lige har logget, kan man altså ikke se hvilken dag man er på. Skærmbillede: `f-390x844-06-pr-toast.png`.

**F6. Tryk på en dag i strimlen forlader forsiden** (åbner passet i Program-fanen), og tryk på i dag/hviledage gør ingenting og giver intet svar. Der er ingen måde at se en anden dags pas på forsiden. Ikke en fejl mod ordre 330, men "den valgte dag" kan kun være passets dag.

**F7. "Mere" ligger under folden** (samme årsag som F3; kun lav, fordi et lukket "Mere" ikke skjuler noget vigtigt).

**F8. Pause-linjen (fast, 48 px) overlapper bundnavigationen med 4 px** (742–790 mod nav fra 786 på 390x844; 678–726/722 på 360). Kosmetisk.

## Hvad der holdt (kort)

- I dag = udfyldt lys cirkel + `aria-current`, viste dag = 2 px kant + fed + trekant, pas/klaret/hvile skilles på form (fast/✓/stiplet), 8,0 px luft på begge viewports, ingen vandret rulning. Overskriften følger passets dag ("Tirsdag …" på en onsdag, undertekst "I dag er det onsdag").
- "Mere" åbner/lukker (`aria-expanded`), "Mit program" står bag den, Pause-linje, "Næste øvelse" og "Ret sæt" findes.
- Tal: over folden 31 (390x844) og 28 (360x780) ting med en mock på fire dage; hele siden 32 ting/1045 px lukket mod 117 ting/3423 px åben. RAPPORT-330's "30 over folden" er altså rigtig i størrelsesorden; forskellen 105 → 32 hele siden holder.
- Skak: alle 14 punkter grønne, se `outputs/334/skak-resultat.json`.

## Hvad jeg ikke kunne teste

- Rigtig telefon/rigtig touch-rulning; alt er headless Chromium med mock. Toast-levetid er målt, ikke oplevet.
- "Før"-tallene i RAPPORT-330 (105/30/28) er ikke genmålt: jeg har ikke kørt `f68ebdf` igen.
- Forsiden med en dag hvor i dag har et pas (kun onsdag-mock, hvor i dag er hviledag) og med ugedagsløse (fleksible) pas.
- Skak: Edge, mobilbredde for Opstil-drag (kørt på 1280 px, som roegtest-326 selv gør), "Find mat" gennemspillet (kun fanen åbnes), print-ark, `roegtest-dag5` (326 selv kunne ikke bekræfte den).
- Skak-repoets arbejdstræ har ucommittede ændringer (`skak.html`, `src/braet.js`, `src/styles.css`, `src/udseende.js`), som ikke er 326's; jeg testede main (`288c95a`) og rørte intet der.
